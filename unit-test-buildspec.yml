version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - cd frontend
      - npm install
      - npm install @angular/cli

  build:
    commands:
      - npm run test -- --no-watch --no-progress --browsers=ChromeHeadlessCI